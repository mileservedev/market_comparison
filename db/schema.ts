import { index, sqliteTable, text, uniqueIndex } from 'drizzle-orm/sqlite-core';
export const votes = sqliteTable('votes', { id: text('id').primaryKey(), feature: text('feature').notNull(), product: text('product').notNull(), fingerprint: text('fingerprint').notNull(), ipAddress: text('ip_address').notNull(), userAgent: text('user_agent').notNull(), deviceDetails: text('device_details').notNull(), createdAt: text('created_at').notNull(), updatedAt: text('updated_at').notNull() }, (table) => [uniqueIndex('idx_votes_fingerprint_feature').on(table.fingerprint, table.feature), index('idx_votes_feature_product').on(table.feature, table.product)]);
